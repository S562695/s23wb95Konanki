extends layout

block content
  h1= title
  p Welcome to Naga Lakshmi's world#{NagaLakshmiWorld}
  <input class="btn-btn-secondary" type="button" value="Press Me" onclick="doReduce()">
  div(id="reduce") Reduce the value
  script.
    let count=20;
    let change =-1;
    function doReduce(){
      console.log("Enter")
      count=count+change;
      change=change-1;
      document.getElementById('reduce').innerHTML=`Count ${count} with Change ${change}`
    }